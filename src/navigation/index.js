import React from "react";
import { NavigationContainer } from "@react-navigation/native";
import Main from "./Main";

const Root = () => {
  return (
    <NavigationContainer>
      <Main />
    </NavigationContainer>
  );
};

export default Root;
